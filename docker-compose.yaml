services:
  db:
    image: postgres:16

    env_file:
      - .env

  backend:
    build: ./backend

    env_file:
      - .env

    depends_on:
      - db

  frontend:
    build: ./frontend

    depends_on:
      - backend
